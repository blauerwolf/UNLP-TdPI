{
    El Correo Argentino necesita procesar los envios entregados durante el mes de julio de 2024.
    De cada envío se conoce el código del cliente, día, código postal y peso del paquete.

    a) Implementar un módulo que lea envíos, genere y retorne un árbol binario de búsqueda ordenado
    por código postal, donde para cada código postal se almacenen en una lista todos los envíos
    (código de cliente, día y peso del paquete) correspondientes. La lectura finaliza con código de
    cliente 0.

    b) Implementar un módulo que reciba la estructura generada en a) y un código postal, y retorne
    todos los envíos de dicho código postal.

    c) Realizar un módulo recursivo que reciba la estructura que retorna el inciso b) y retorne los
    dos códigos de cliente corresondientes al envío con mayor y menor peso.

    NOTA: Implementar el programa principal, que invoque a los incisos a, b, y c.

}